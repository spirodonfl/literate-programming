Configuration values stored here especially since this part of the lit-parser and we want to demonstrate how to handle configuration values in a markdown file.

``` lit-type:config lit-name:general
output_source=false
output_source_absolute_paths=false
```

``` lit-type:config lit-name:null
helpers\Helpers.md
```